// import { AxiosResponse } from 'axios'
// import { LoginResponse } from '@/api/types/user.type'
// import httpClient from '../http'

export default class UserService {
	// static async getUser(email: string, password: string): Promise<AxiosResponse<LoginResponse>> {
	// 	return httpClient.post<LoginResponse>('/auth/login', { email: email, password: password })
	// }
}